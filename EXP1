#include <stdio.h>

int main() {
   int a, b, c, max;
   printf("REG NO:25331A05F7\n");
   printf("enter the numbers:");
   scanf("%d %d %d", &a, &b, &c);
   max = (a > b) ? (a > c ? a : c) : (b > c ? b : c);
   printf("max=%d", max);
   return 0;
}
