#include <stdio.h>
#include <math.h>

int main() {
   float p, r, t, si, ci;
   printf("REG NO:25331A05F7\n");
   printf("enter p,r,t:");
   scanf("%f %f %f", &p, &r, &t);

   si = (p * r * t) / 100;
   ci = p * pow((1 + r/100), t) - p;

   printf("SI = %.2f\nCI = %.2f", si, ci);
   return 0;
}
